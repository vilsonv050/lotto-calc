# Общий контекст проекта Codex GPT

Этот файл — общая память проекта для работы с разных компьютеров и из разных чатов Codex. GitHub-репозиторий `vilsonv050/lotto-calc`, ветка `main`, является единственным источником истины. Чаты сами по себе не синхронизируют память: в начале работы нужно прочитать этот файл, а после значимого изменения — обновить его в том же коммите, что и рабочие файлы.

## Назначение проекта

В папке `Codex GPT` разрабатываются автономные JavaScript-bookmarklet-скрипты для исследования `https://nloto.ru/`. Основной текущий скрипт собирает статистику тиражей, комбинации, финансовые показатели и ссылки на официальные результаты.

Работа первоначально велась на другом компьютере в `E:\Codex GPT`. 30 июля 2026 года файлы были сохранены в GitHub, чтобы продолжать работу с нескольких компьютеров без ручного переноса.

## Текущая каноническая версия

- Файл: `Статистика Тиражей_03-08-26_V1.txt`
- Версия внутри скрипта: `NLOTO STATISTICS v7.7`
- Коммит: текущий `HEAD` ветки `main`, содержащий этот файл и данное обновление контекста.
- Ветка: `main`
- Это текущая рабочая версия. Исторические версии не изменены.
- Исполняемая копия для существующего загрузчика NLOTO v2.5.8: `Scripts/Статистика Тиражей v7.7.js`. Она побайтно соответствует каноническому исходнику после удаления только начального префикса `javascript:`.
- В `Scripts` находятся базовая `Статистика Тиражей.txt` (v6.0), предыдущая стабильная `Статистика Тиражей v7.6.js` и новая рабочая `Статистика Тиражей v7.7.js`. После подтверждения live-запуска v7.7 предыдущую v7.6 можно убрать отдельным решением пользователя.
- Исторические датированные версии и однострочная URL-сборка сохранены только в `Codex GPT`.
- Канонический файл для дальнейшей разработки остаётся в `Codex GPT`; загрузчик NLOTO не изменяется.

## Восстановленная хронология

### Исходный файл

`Статистика Тиражей.txt` был первым рабочим вариантом. Он сохранён как исторический исходник и не должен изменяться.

### Версия с датой — `Статистика Тиражей_29-07-26.txt`

Основные изменения:

- архив загружается страницами до 200 тиражей вместо перебора каждого номера;
- добавлены режимы полной и быстрой загрузки;
- добавлены настоящая отмена запросов через `AbortController` и повтор неудачных запросов;
- номера тиражей стали кликабельными ссылками на официальный результат;
- статистика адаптирована под лотереи с одним, двумя и тремя игровыми полями;
- даты и экспорт CSV нормализованы;
- известные `productId` были сверены с фактической структурой сайта.

### V1 — `Статистика Тиражей_29-07-26_V1.txt`

Исправлена загрузка финансовых данных:

- полной статистике возвращён режим по умолчанию;
- исправлен источник детальных данных;
- комбинации корректно объединяются с количеством билетов, выплатами и другими финансовыми полями;
- устаревший кэш не сохраняет прочерки как корректные данные.

Проверочный пример «Большая 8»: 573 билета, расчётная выручка 143 250 ₽, выплаты 85 950 ₽, 60%.

### V2 — `Статистика Тиражей_29-07-26_V2.txt`

Исправлена неоднозначность одинаковых названий лотерей:

- старые, текущие и будущие версии группируются по базовому названию;
- варианты явно подписываются как «ДО обновления», «ПОСЛЕ обновления», «СТАРАЯ», «НОВАЯ» и содержат `productId`;
- выбор лотереи заменён компактным раскрывающимся списком;
- хронологический порядок вариантов проверен на «Великолепной 8» и «Премьере».

Пример: `101051` — версия «Великолепной 8» до обновления, `109031` — новая версия после обновления, пока без тиражей.

### V3 — `Статистика Тиражей_29-07-26_V3.txt`

Исправлена системная ошибка расчёта исторической выручки:

- цена берётся из конкретного тиража, а не из текущей цены лотереи;
- добавлены колонки «Цена билета» и «Суперприз»;
- показатель `ROI` переименован в точное «Выплата%»;
- обновлён CSV-экспорт;
- компактный список лотерей сохранён;
- логика проверена на разных типах лотерей.

Контрольный тираж «Пятая скорость» №024581:

- билетов: 325 593;
- историческая цена билета: 10 ₽;
- выручка: 3 255 930 ₽;
- выигрыш: 3 523 146 ₽;
- суперприз: 1 342 020 ₽;
- выплата: примерно 108,21%.

### v7.4 — `Статистика Тиражей_30-07-26.txt`

Реализован утверждённый компактный интерфейс:

- все основные фильтры собраны в верхней панели;
- сохранены максимум тиражей, интервал дат и полный/быстрый режим;
- добавлены сводные карточки над таблицей;
- дата и время тиража разделены;
- отдельная колонка «Время» добавлена в таблицу и CSV;
- компоновка адаптируется к широкому и узкому окну без переполнения корневой панели.

### v7.5 — `Статистика Тиражей_30-07-26_V1.txt`

Улучшена проверка последнего тиража:

- кнопка «Проверить последний тираж» перенесена рядом с выбором лотереи;
- на узком экране кнопка автоматически занимает отдельную строку;
- найденный номер, дата, время, цена и суперприз показываются в широкой строке под верхней панелью;
- подробный технический журнал скрыт по умолчанию и раскрывается кнопкой «Детали»;
- расчёты, фильтры, таблица и экспорт v7.4 сохранены без изменений.

### v7.6 — `Статистика Тиражей_03-08-26.txt`

Подготовлен корректный формат для запуска из URL закладки:

- пользователь сообщил, что многострочный v7.5 запускается из консоли, но не из закладки;
- исходный v7.5 содержал 1 767 строк, поэтому при переносе в одно поле URL браузер мог сохранить его с переносами или не полностью;
- читаемый исходник сохранён отдельным файлом v7.6;
- из него собран `Статистика Тиражей_03-08-26_ЗАКЛАДКА_URL.txt`, содержащий весь bookmarklet ровно в одной строке и начинающийся с `javascript:`;
- функциональная логика и интерфейс v7.5 не изменялись.

### v7.7 — `Статистика Тиражей_03-08-26_V1.txt`

Добавлен отдельный блок будущих пустых тиражей:

- сайт отдаёт 10 актуальных и будущих записей через `/api/v2/products/{productId}/draws/actual?nums={drawNum}`;
- будущие записи содержат номер, дату и время розыгрыша, окончание продаж, цену, признак страховки, акцию и её описание; у некоторых игр присутствует объект дополнительных призов;
- текущий суперприз берётся из актуальной карточки лотереи в каталоге, поскольку в массиве будущих записей поле `jackpot` может быть равно нулю;
- будущие записи хранятся отдельно от завершённых и не участвуют в расчётах частот, выручки, выигрышей и выплат;
- блок загружается как при проверке последнего тиража, так и при обычной загрузке статистики;
- для лотереи без завершённых тиражей интерфейс всё равно может показать доступные будущие записи.

## Последнее незавершённое направление

Для существующего загрузчика NLOTO v2.5.8 подготовлен `Scripts/Статистика Тиражей v7.7.js` без префикса `javascript:`. Следующий шаг — в загрузчике выполнить синхронизацию с GitHub, выбрать «Статистика Тиражей v7.7» и запустить её на `https://nloto.ru/`. Нужно подтвердить показ 10 будущих тиражей для выбранной лотереи и обычную загрузку завершённой статистики. Загрузчик NLOTO не изменяется.

## Правило версий файлов

- Исторические версии не перезаписывать.
- Первая новая версия за день: `Статистика Тиражей_ДД-ММ-ГГ.txt`.
- Следующие версии того же дня: `_V1`, `_V2`, `_V3` и далее.
- Перед созданием файла проверить существующие имена в папке и выбрать следующий свободный номер.

## Протокол синхронизации между компьютерами и чатами

1. Перед началом работы проверить чистоту рабочей копии и получить свежий `main` из GitHub.
2. Прочитать `AGENTS.md` и этот файл полностью.
3. Работать только от последней версии в `main`.
4. После значимой работы обновить разделы «Текущая каноническая версия», «Последнее незавершённое направление» и журнал ниже.
5. Коммитить рабочий файл и обновление контекста вместе.
6. Не хранить здесь пароли, токены, cookies, персональные данные или полный сырой лог инструментов.
7. Не использовать force-push. При расхождении двух компьютеров сначала сохранить локальные изменения, затем выполнить обычное получение и rebase/merge с ручным разрешением конфликта.
8. Использовать `SYNC_GITHUB.ps1 status` для проверки, `SYNC_GITHUB.ps1 pull` перед работой и `SYNC_GITHUB.ps1 push` после проверенного коммита. На Windows запускать через `powershell -ExecutionPolicy Bypass -File .\SYNC_GITHUB.ps1 <действие>`.

## Журнал продолжения

### 2026-07-30 — восстановление и перенос общей памяти

- Переписка с другого компьютера восстановлена из Word-документа.
- Подтверждено, что GitHub `main` содержит исходник и версии с датой, V1, V2 и V3.
- Подтверждён последний коммит V3: `416be45ea6c776c7db7e671f45a3d1fae2993288`.
- Обнаружена отдельная устаревшая ветка `github--привязка-4d989`; она относится к другому файлу и не является основой для `Codex GPT`.
- Создан этот общий файл контекста для двух компьютеров и разных чатов.

### 2026-07-30 — настройка синхронизации на текущем компьютере

- На GitHub добавлен `AGENTS.md`, который заставляет новые чаты читать и обновлять общий контекст.
- Добавлен переносимый `SYNC_GITHUB.ps1` с командами `status`, `pull` и `push`.
- Исправлена совместимость helper-файла с Windows PowerShell 5.1.
- Локальная копия создана вне OneDrive: `C:\\Users\\vilso\\Documents\\lotto-calc`.
- В sparse checkout включена только рабочая папка `Codex GPT`.
- GitHub-аккаунт `vilsonv050` авторизован через Git Credential Manager.
- Контрольные команды `status`, `pull` и `push` прошли успешно; локальный `main` совпал с `origin/main`.
- На другом компьютере нужно авторизовать тот же GitHub-аккаунт, клонировать репозиторий, открыть `Codex GPT` и выполнить `SYNC_GITHUB.ps1 pull` перед работой.

### 2026-07-30 — компактный интерфейс v7.4

- Пользователь утвердил первый макет — компактную панель.
- Создан новый файл `Статистика Тиражей_30-07-26.txt`; V3 сохранён без изменений.
- Верхние секции заменены компактной панелью: лотерея, ручной `productId`, проверка последнего тиража, максимум тиражей, даты «от/до», режим и кнопка загрузки.
- Добавлены шесть сводных карточек: билеты, цена билета, выручка, выигрыш, суперприз и выплата%.
- Дата и время тиража разделены в таблице и CSV; добавлена отдельная колонка «Время».
- Интерфейс адаптирован к широкому и узкому окну. На контрольной ширине 635 px переполнения корневого окна и рабочей области нет; широкая таблица и карточки прокручиваются внутри своих блоков.
- Синтаксис проверен через `node --check`.
- На локальном браузерном стенде проверены полный режим, интервал `28.07.2025–30.07.2025`, три тиража и все финансовые колонки.
- Контрольный тираж №024581 дал: время `13:00`, 325 593 билета, цена 10 ₽, выручка 3 255 930 ₽, выигрыш 3 523 146 ₽, суперприз 1 342 020 ₽, выплата 108,2%.
- Ограничение проверки: автоматическая вставка bookmarklet на live-страницу запрещена защитой встроенного браузера, а локальный стенд использовал детерминированные ответы в форме API NLOTO. Финальную live-проверку должен инициировать пользователь запуском bookmarklet на `nloto.ru`.

### 2026-07-30 — копия v7.4 в Scripts

- По просьбе пользователя создана копия `Scripts/Статистика Тиражей_30-07-26.txt`.
- Копия побайтно совпадает с каноническим файлом из `Codex GPT`; SHA-256 обоих файлов: `821A91FE90596FCDE10B709FE0B050C96C999CBC19B1914C1998B8D03716C1F9`.
- Последующие изменения сначала выполнять в новой версии внутри `Codex GPT`, а затем при необходимости обновлять соответствующую копию в `Scripts`.

### 2026-07-30 — читаемая проверка последнего тиража v7.5

- По решению пользователя узкая отдельная карточка проверки убрана.
- Кнопка проверки размещена рядом с выбором лотереи, а результат вынесен в полноширинную строку.
- Краткий результат содержит номер, дату, время, цену и суперприз; технические сообщения доступны через «Детали».
- На ширине 635 px корневое окно и рабочая область не имеют горизонтального переполнения.
- Синтаксис проверен через `node --check`.
- На локальном браузерном стенде проверены успешная проверка последнего тиража, раскрытие/скрытие деталей и полная загрузка трёх тиражей.
- Контрольный тираж №024581 остался неизменным: время `13:00`, выручка 3 255 930 ₽, выигрыш 3 523 146 ₽, суперприз 1 342 020 ₽, выплата 108,2%.
- Подготовлена точная копия `Scripts/Статистика Тиражей_30-07-26_V1.txt`.

### 2026-08-03 — однострочная URL-закладка v7.6

- Пользователь сообщил, что v7.5 работает при запуске из консоли, но не запускается как URL закладки.
- Подтверждено, что рабочий файл v7.5 многострочный: 1 767 строк.
- Создан новый читаемый исходник `Статистика Тиражей_03-08-26.txt` с внутренней версией `NLOTO STATISTICS v7.6`; логика v7.5 не изменена.
- Создана производная сборка `Статистика Тиражей_03-08-26_ЗАКЛАДКА_URL.txt`: весь код находится в одной строке и начинается с `javascript:`.
- Обе версии проверены компиляцией JavaScript; в URL-сборке отсутствуют символы CR/LF.
- Читаемый исходник и URL-сборка скопированы в `Scripts`.
- Ограничение проверки: защитный механизм встроенного браузера не позволяет автоматически выполнить URL вида `javascript:` на live-странице. Финальный запуск должен вручную выполнить пользователь на `nloto.ru`.

### 2026-08-03 — запуск v7.6 через существующий загрузчик NLOTO

- Пользователь предоставил рабочий загрузчик NLOTO v2.5.8 и запретил изменять его.
- Установлено, что файлы, начинающиеся с `javascript:`, загрузчик повторно запускает как URL через скрытую ссылку; для большого v7.6 это сохраняет проблему длины URL.
- Создан `Scripts/Статистика Тиражей v7.6.js` без начального префикса `javascript:`. Поэтому загрузчик выбирает свою ветку выполнения обычного JavaScript через элемент `<script>`.
- Код интерфейса, расчётов и запросов v7.6 не изменён. Исполняемый файл точно равен каноническому исходнику после удаления первых 11 символов `javascript:`.
- Синтаксис исполняемого `.js` проверен через `node --check`; отдельно подтверждено, что проверка загрузчика на `javascript:` для него возвращает ложь.
- Загрузчик, его настройки и токен не изменялись. Финальный запуск через интерфейс загрузчика на `nloto.ru` должен выполнить пользователь.

### 2026-08-03 — очистка списка статистики в Scripts

- По решению пользователя в папке `Scripts` оставлены только `Статистика Тиражей.txt` и `Статистика Тиражей v7.6.js`.
- Из `Scripts` удалены датированные копии `Статистика Тиражей_30-07-26.txt`, `Статистика Тиражей_30-07-26_V1.txt`, `Статистика Тиражей_03-08-26.txt` и `Статистика Тиражей_03-08-26_ЗАКЛАДКА_URL.txt`.
- Исторические и канонические файлы в `Codex GPT` не удалялись.
- Удалённые копии остаются восстановимыми из истории Git.

### 2026-08-03 — будущие пустые тиражи v7.7

- На открытом `nloto.ru` подтверждено, что список выбора тиража содержит ровно 10 актуальных и будущих записей.
- Для «Большой 8» проверены №071203–071212 с шагом 15 минут и акцией «Распред. день».
- Для «Мечталлиона» проверены 10 еженедельных тиражей №000204–000213.
- Для «Цветных шаров» проверены 10 тиражей №016658–016667, страховка и дополнительные призовые игры.
- Установлен фактический API: `/api/v2/products/{productId}/draws/actual?nums={drawNum}`.
- Создан новый канонический файл `Статистика Тиражей_03-08-26_V1.txt` с внутренней версией `NLOTO STATISTICS v7.7`.
- Добавлена исполняемая копия `Scripts/Статистика Тиражей v7.7.js`; загрузчик NLOTO v2.5.8 не изменялся.
- Синтаксис проверен компиляцией JavaScript.
- На локальном браузерном стенде проверены DIGITAL («Большая 8»), BINGO («Мечталлион») и MULTIGAME («Цветные шары»): в каждом случае показано 10 будущих строк.
- Дополнительно подтверждено, что после загрузки завершённой статистики будущий блок остаётся видимым, а финансовая таблица завершённых тиражей сохраняет прежние расчёты.
- Ограничение: окончательный live-запуск через интерфейс существующего загрузчика на `nloto.ru` выполняет пользователь.

### 2026-08-03 — отдельное локальное приложение NLOTO

- По просьбе пользователя статистика вынесена в отдельное локальное приложение: для работы больше не требуется открывать страницу `nloto.ru`.
- Создана папка `Codex GPT/NLOTO Local App` с файлами запуска, остановки, локальной страницей, сервером и инструкцией.
- Запуск выполняется двойным щелчком по `START_NLOTO_APP.cmd`; приложение открывается по адресу `http://127.0.0.1:8787`.
- Локальный сервер работает средствами Windows PowerShell, слушает только `127.0.0.1` и не требует установки Python или Node.js.
- Сервер отдаёт существующий исполняемый файл `Scripts/Статистика Тиражей v7.7.js`, поэтому интерфейс и расчёты не дублируются.
- Запросы `/api/...` и CDN-запросы v7.7 перенаправляются через ограниченный прокси только на официальный адрес `static.nationallottery.ru/_next/image`.
- Проверены реальные ответы официального API: каталог, словари, история, детали тиража и 10 будущих тиражей.
- В локальном интерфейсе для «Большой 8» успешно показаны 10 будущих тиражей и полностью загружены 20 завершённых тиражей; ошибок браузера не обнаружено.
- Для остановки используются кнопка «Остановить приложение» или `STOP_NLOTO_APP.cmd`.
- После синхронизации GitHub приложение переносимо на другой Windows-компьютер: абсолютные пути текущего компьютера в него не вшиты.
